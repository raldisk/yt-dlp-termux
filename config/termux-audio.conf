# termux-audio.conf — yt-dlp configuration for Termux (audio-only mode)
# Managed by: yt-dlp-termux
# Accessible from the launcher as: option 3 (Audio only URL)
#
# item 2.2 Option B: restored to launcher menu as option 3.
# Personal settings belong in user.conf — NOT in this file.

# ─── POT Provider ────────────────────────────────────────────────────────────
--extractor-args "youtubepot-bgutilhttp:base_url=http://127.0.0.1:4416"

# ─── YouTube extractor arguments ─────────────────────────────────────────────
--extractor-args "youtubetab:skip=authcheck;youtube:formats=dashy;youtube:skip=hls;youtube:player_client=android_sdkless,mweb,web_creator,tv_embedded,web_safari,web_embedded;youtube:player_js_version=actual;youtube:fetch_pot=always;youtube:playback_wait=3;youtube-ejs:jitless=true"

# ─── JavaScript runtimes ──────────────────────────────────────────────────────
--extractor-args "youtube:jsinterp_order=node,quickjs:/data/data/com.termux/files/usr/bin/qjs,deno:/data/data/com.termux/files/home/.deno/bin/deno"

# ─── Audio-only format ───────────────────────────────────────────────────────
-f "ba[acodec^=mp4a]/ba/bestaudio"
-x
--audio-format m4a
--audio-quality 0

# ─── Network ─────────────────────────────────────────────────────────────────
-N 4
--socket-timeout 30
--retries 10
--fragment-retries 10
--retry-sleep 5

# ─── Output template ─────────────────────────────────────────────────────────
--paths "/data/data/com.termux/files/home/storage/shared/Termux-Extension-Archive"
-o "%(channel)s [%(channel_id)s]/Audio/REL_%(upload_date>%Y%m%d,release_date>%Y%m%d,epoch>%Y%m%d)s DL_%(epoch+28800>%Y%m%d_%H%M%S)s %(title).100B [%(extractor_key)s %(id)s].%(ext)s"

# ─── Metadata ────────────────────────────────────────────────────────────────
--embed-thumbnail
--embed-metadata
--convert-thumbnails jpg

# ─── Progress ────────────────────────────────────────────────────────────────
--progress
--console-title
--newline

# ─── Error handling ──────────────────────────────────────────────────────────
--fixup never
--ignore-errors
--no-abort-on-error

# ─── Cache ───────────────────────────────────────────────────────────────────
--cache-dir "/data/data/com.termux/files/home/.cache/yt-dlp"
